# Outreach exports

CSV batches of hotels for contact-finding (Apollo and similar). Generated by
`scripts/export-outreach-batch.mjs`, which is **read-only** against Supabase and
touches no breakout, baseline or engagement logic.

## Batches

| File | Scope | Hotels | Generated |
|---|---|---|---|
| `apollo-batch-01-uk-ireland.csv` | tracked hotels in the United Kingdom + Ireland | 21 | 2026-07-30 |

## Regenerating / new batches

```bash
node scripts/export-outreach-batch.mjs
```

```bash
node scripts/export-outreach-batch.mjs --countries "France,Italy" --limit 30 --out outreach/apollo-batch-02-france-italy.csv
```

## Selection rules

- `hotels.tracked = true`
- has at least one row in `posts` (matched on `instagram_handle`)
- has a non-blank `hotels.website`
- `hotels.country` matches the requested list
- ordered by posts in the last 90 days, then by follower count

## Columns

| Column | Source |
|---|---|
| `hotel_id` | `hotels.id` — the FK to use when inserting into `contacts` |
| `hotel_name` | `hotels.name` |
| `website` | `hotels.website` (always a full `https://` URL) |
| `root_domain` | derived from `website` — **this is the field Apollo wants** |
| `country`, `region` | `hotels.country` / `hotels.region` |
| `instagram_handle` | `hotels.instagram_handle` |
| `followers` | `profile_snapshots.followers_count`, latest snapshot per handle |
| `posts_last_90d`, `posts_total`, `last_posted` | plain counts over `posts.posted_at` |
| `accreditation_lists` | `hotels.sources` |
| `gold_list_editions` | `hotels.gold_list_listings`, e.g. `US 2025, UK 2025` |

## Watch out: shared root domains

Group-owned properties share a root domain, so Apollo will return the group's
contacts rather than the individual hotel's. In batch 01:

- `maybourne.com` × 3 — Claridge's, The Connaught, The Berkeley
- `fourseasons.com` × 2 — Tower Bridge, Park Lane
- `dorchestercollection.com` × 2 — The Dorchester, Coworth Park

For these, search Apollo by the group domain and filter by job title/location,
or find the property's social lead on LinkedIn directly.

## Country data is dirty

`hotels.country` stores the US two ways — `United States` (89) and `USA` (39) —
and `region` has both `Asia-Pacific` (127) and `Asia` (35). UK/Ireland is clean,
so batch 01 is unaffected, but any future US or Asia batch must match both
spellings or it will silently miss hotels.
